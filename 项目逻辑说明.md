# 穿越团 · 主神空间终端 — 项目逻辑说明

> 版本：v1.1 | 技术栈：Electron + 原生 JS + 纯 CSS 暗色主题
> 说明日期：2026-05-30

---

## 一、项目架构总览

### 1.1 分层结构

```
┌─────────────────────────────────────────────────┐
│  页面层 (index.html)                             │
│  6 个页面容器，通过 display: none/active 切换     │
├─────────────────────────────────────────────────┤
│  逻辑层 (scripts/)                               │
│  app.js — 导航、数据 I/O、DM 模式开关            │
│  character.js — 角色面板全部逻辑                  │
│  shop.js — 商店 + 剑侠世界                        │
├─────────────────────────────────────────────────┤
│  样式层 (styles/theme.css)                       │
│  暗色主题、布局、动画、粒子特效                    │
├─────────────────────────────────────────────────┤
│  数据层 (data/)                                  │
│  characters.json — 角色持久化                     │
│  shop_items.json — 商店商品                       │
│  jianxia_items.json — 剑侠世界观                  │
│  status_refs/ — 血脉、种族、职业、一转参考数据      │
├─────────────────────────────────────────────────┤
│  运行层                                          │
│  main.js — Electron 主进程                       │
│  preload.js — contextBridge API 桥接              │
│  server.js — 开发 HTTP 服务器                     │
└─────────────────────────────────────────────────┘
```

### 1.2 启动方式

| 方式 | 命令 | 说明 |
|------|------|------|
| Electron 桌面应用 | `npm start` | 需要先 `npm install` |
| HTTP 开发服务器 | `node server.js` | 浏览器访问 `localhost:3000`，推荐调试用 |
| 直接双击 HTML | — | 因 `file://` 阻止 `fetch()`，数据无法加载 |

### 1.3 页面路由

```
主页 (#mainPage)
  ├── 🧙 角色面板 (#characterPage)   ← 核心页面
  ├── 🏪 商店面板 (#shopPage)
  └── 🗡️ 剑侠情缘 (#jianxiaPage)
```

路由由 `navigateTo(page)` 驱动：隐藏所有 `.page`，显示目标 `#${page}Page`，调用对应初始化函数。

---

## 二、数据流（Data Flow）

### 2.1 加载流程

```
应用启动
  │
  ├─ DOMContentLoaded
  │    ├─ initNavigation()      — 绑定导航按钮
  │    ├─ initCharSelector()    — 角色下拉框监听
  │    └─ loadAllData()         — 读取 characters.json
  │         │
  │         ├─ characters.json → characters 全局对象
  │         │
  │         └─ 用户导航到某页面
  │              ├─ character → initCharacterPage()
  │              │    ├─ 构建 DOM (identity / stats / skills / ...)
  │              │    ├─ loadStatusRefs() → 加载四项参考 JSON
  │              │    └─ refreshCharSelector() → 加载角色
  │              │
  │              ├─ shop → initShopPage() → loadShopData()
  │              │    ├─ shop_items.json → _shopItems
  │              │    └─ jianxia_items.json → _jianxiaItems (合并展示)
  │              │
  │              └─ jianxia → loadJianxiaData()
  │                   └─ jianxia_items.json → filterJianxia()
  │
  └─ 加载路径
       ├─ Electron 模式: electronAPI.readFile() → Node.js fs
       └─ 浏览器模式: loadJSON() → fetch() → 失败时使用内联默认数据
```

### 2.2 保存流程

```
用户编辑任意字段
  │
  ├─ onCharFieldChange(key)
  │    ├─ 更新全局 _mainXp / _mainAp
  │    └─ gatherCharData() → 收集所有 UI 字段为对象
  │         ├─ identity       (玩家名称、角色名、种族、性别等)
  │         ├─ stats          (STR/DEX/CON/INT/WIS/CHA)
  │         ├─ derived        (等级、AC、先攻、速度、HP/疲劳、XP)
  │         ├─ skills         (所有熟练项勾选+值)
  │         ├─ background     (个性/特点/理念/牵绊/缺点)
  │         ├─ equipment      (武器/防具)
  │         ├─ wealth         (货币)
  │         ├─ inventory      (物品清单)
  │         ├─ activeSlots    (主动技能)
  │         ├─ passiveSlots   (被动技能)
  │         ├─ talents        (天赋标签)
  │         ├─ enhances       (强化标签)
  │         └─ statusDetail   (血脉/种族/职业/一转/转职状态)
  │
  ├─ characters[currentCharName] = gatherCharData()
  │
  └─ autoSave() → saveAllData()
       └─ characters.json (通过 electronAPI 或 fetch)
```

### 2.3 核心数据结构 (characters.json)

```javascript
{
  "角色名": {
    identity:    { playerName, charName, race, gender, age, height, weight, eyeColor, skinColor, hairColor },
    stats:       { str, dex, con, int, wis, cha },           // 核心六属性 1-30
    derived:     { level, ac, initiative, speed, attackBonus,
                   hpCurrent, hpMax, fatigueCurrent, fatigueMax,
                   xpCurrent, xpMax },                        // 衍生属性
    skills:      { "skillId": { checked: bool, value: number } },  // 熟练项
    customNames: { "customSkillId": string },                 // 自定义熟练项名称
    otherProfs:  string[],                                    // 其他熟练项（如"通用语"）
    background:  { bgPersonality, bgTraits, bgIdeals, bgBonds, bgFlaws, bgMechanics },
    talents:     string[],                                    // 天赋（标签形式）
    enhances:    string[],                                    // 强化（标签形式）
    equipment:   { weaponMain, weaponOff, armor },            // 装备
    wealth:      { currencies: [{name, amount}], achievePoints },
    inventory:   [{ name, qty, weight }],                     // 物品清单
    activeSlots: [{ name, castTime, range, duration, cost, category }],  // 主动技能
    passiveSlots: [{ name, category, stage }],                // 被动技能
    property:    { mainXp, mainAp, badges: {} },              // 主神空间财产
    statusDetail: { bloodline, race, baseClass, promotion, promoState }  // 生存状态参考
  }
}
```

---

## 三、角色页面实现逻辑（核心）

角色页面是项目中最复杂的页面，分为 6 个功能区块。下面是每个区块的详细逻辑。

### 3.1 初始化流程

```
initCharacterPage()
  │
  ├─ 无角色? → showCreateGuide() → 显示创建引导页
  │
  ├─ 首次进入?
  │    ├─ buildIdentityGrid()    构建身份信息区
  │    ├─ buildStatsGrid()       构建核心属性区
  │    ├─ buildDerivedGrid()     构建衍生属性区
  │    ├─ buildStatusDisplay()   构建生存状态区
  │    ├─ buildSkillLists()      构建技能熟练项区
  │    ├─ buildBackgroundFields()构建背景与特质区
  │    ├─ buildBadgeGrid()       构建铭牌区
  │    └─ buildCurrencyGrid()    构建货币区
  │
  ├─ loadStatusRefs() → 加载下拉参考数据
  │    ├─ bloodline.json  → #charBloodline 下拉框
  │    ├─ race.json       → #charRace 下拉框
  │    ├─ base_class.json → #charBaseClass 下拉框
  │    └─ promotion.json  → #charPromotion 下拉框
  │
  └─ refreshCharSelector()
       └─ 触发 loadCharacterIntoUI(name, data)
```

### 3.2 区块 1 — 基础身份 (Identity)

**DOM 位置**: `#identityGrid`
**构建函数**: `buildIdentityGrid()`
**数据字段**: `identity.*`

由 10 个字段组成的 5 列网格：

| 字段 key | 标签 | 类型 | 范围 |
|---------|------|------|------|
| playerName | 玩家名称 | text | — |
| charName | 角色名称 | text | — |
| race | 种族 | text | — |
| gender | 性别 | text | — |
| age | 年龄 | number | 1-999, 默认20 |
| height | 身高(cm) | number | 1-300, 默认170 |
| weight | 体重(kg) | number | 1-500, 默认65 |
| eyeColor | 瞳色 | text | — |
| skinColor | 肤色 | text | — |
| hairColor | 发色 | text | — |

**编辑机制**: 每个字段绑定 `oninput` → `onCharFieldChange(key)`。数值类型带 `-` / `+` 步进按钮（`stepper` 组件）。

---

### 3.3 区块 2 — 核心属性 (Ability Scores)

**DOM 位置**: `#statsGrid` + `#derivedGrid`
**构建函数**: `buildStatsGrid()` + `buildDerivedGrid()`

**六属性**（标准 D&D 风格）：

| 属性 | ID | 默认值 | 范围 |
|------|----|--------|------|
| 力量 | str | 10 | 1-30 |
| 敏捷 | dex | 10 | 1-30 |
| 体质 | con | 10 | 1-30 |
| 智力 | int | 10 | 1-30 |
| 感知 | wis | 10 | 1-30 |
| 魅力 | cha | 10 | 1-30 |

每个属性显示：
- 属性名称（如 "STR（力量）"）
- 带步进按钮的数值输入框
- 修正值（`mod = floor((value - 10) / 2)`，如 `+0`, `+2`, `-1`）

**衍生属性**：

| 字段 key | 标签 | 默认值 |
|---------|------|--------|
| level | 穿越者等级 | 1 |
| ac | 护甲等级 AC | 10 |
| initiative | 先攻值 | 0 |
| speed | 移动速度 | 30 |
| attackBonus | 命中加值 | 0 |

**联动**: `onStatChange()` 同时触发 `updateLoadCalc()` → 负重计算依赖 `str` 值。

---

### 3.4 区块 3 — 生存状态 (Status)

**DOM 位置**: `#statusDisplay`
**构建函数**: `buildStatusDisplay()` → 内部调用 `updateStatusIcons('hp')`、`updateStatusIcons('fatigue')`、`updateXpBar()`

#### 3.4.1 血脉 · 种族 · 职业 · 一转（Reference Selectors）

四个下拉选择器，数据来自 `data/status_refs/` 下的 JSON 文件：

| 下拉框 ID | 数据源 | 默认值 |
|-----------|--------|--------|
| `#charBloodline` | bloodline.json | 人类 |
| `#charRace` | race.json | 人类 |
| `#charBaseClass` | base_class.json | 学生 |
| `#charPromotion` | promotion.json | 无 |
| `#charPromoState` | (硬编码) | 未转职 |

**加载路径**:
1. `loadStatusRefs()` 使用 `loadJSON()` 从文件异步加载
2. 加载失败时使用内联默认数据（硬编码在 `character.js` 中）
3. `populateSelect()` 填充 `<select>` 的 `<option>` 列表，保留 `data-current` 属性

**转职三态**: 一转下拉框旁边有一个状态选择器：
- `未转职` — 初始状态
- `转职中` — DM 审核中状态（在 DM 仪表盘中高亮）
- `已转职` — 完成转职

**联动**: 选择变更 → `onCharFieldChange()` + `updateStatusEffect()` + 保存到 `statusDetail`

---

#### 3.4.2 生命值与疲劳值（Heart/Leaf Icons）

视觉核心组件，用 SVG 心形和叶子图标表示 HP 和疲劳。

**HP 心形逻辑** (`updateStatusIcons('hp')`):

```
当前值: cur, 最大值: max
每颗心 = 10 点 HP
full = Math.floor(cur / 10)    ← 完全填满的心数
part = cur % 10               ← 最后一颗心的填充百分比
total = Math.ceil(max / 10)   ← 总需要的心数

渲染: total 颗心，每颗心用 SVG clipPath 裁剪
  - 前 full 颗: 100% 填充 (红色 #ff4757)
  - 第 full+1 颗: part/10 * 100% 填充
  - 剩余: 0% 填充 (暗红色 #3a1a2a)

百分比文本: (cur/max 00%)
```

**疲劳叶子逻辑** (`updateStatusIcons('fatigue')`):
与心形相同规则，颜色为绿色 `#2ed573`。

**预警系统**:
- HP < 30%: 心形红色闪烁动画 (`hpPulse`)
- 疲劳 > 80%: 叶子橙色闪烁动画 (`fatiguePulse`)
- CSS class `.warning` 控制闪烁，SVG 动画通过 `animation: hpPulse 1.2s`

**编辑机制**:
```html
<input type="number" id="st_hpCur">  <span>/</span>
<input type="number" id="st_hpMax">
```
两个独立输入框，中间用分隔符隔开，绑定 `oninput="onStatusChange('hp')"`。

---

#### 3.4.3 经验值进度条

```html
<div class="status-xp">
  <span class="si-label">✨ 经验值</span>
  <div class="xp-bar-wrap">
    <div class="progress-bar xp-bar">
      <div class="fill" id="xpBarFill" style="width:0%"></div>
    </div>
    <span class="xp-text" id="xpText">0 / 100</span>
  </div>
</div>
```

`updateXpBar(cur, next)` 更新进度条宽度和文字。

---

#### 3.4.4 效果预览

`updateStatusEffect()` 从四个下拉框的当前值，查找对应数据中的 `effect` 字段，拼接为摘要文本：

```
血脉: 无特殊血脉效果 | 种族: 无特殊种族效果 | 职业: 教学相长...
```

---

### 3.5 区块 4 — 背景与特质 + 道果

#### 背景 (Background)

五个多行文本域：

| data key | 标签 |
|---------|------|
| bgPersonality | 个性背景 |
| bgTraits | 个人特点 |
| bgIdeals | 理念 |
| bgBonds | 牵绊 |
| bgFlaws | 缺点 |

额外一个 `#bgMechanics` 文本框用于特殊机制描述。

#### 道果 (Talent & Enhance)

两个标签输入组件：
- **天赋** (`#talentInput`): 回车添加，点击 x 删除
- **已经历强化** (`#enhanceInput`): 同上

数据存储为 `string[]`，渲染为彩色 tag 块。

---

### 3.6 区块 5 — 技能与熟练项 (Skills)

#### 技能分类体系

```
技能树 (SKILL_CATEGORIES):
├── 豁免           (6项: STR/DEX/CON/INT/WIS/CHA 豁免)
├── 运动与体能     (5项: 跳跃/攀爬/游泳/承重/自定义)
├── 敏捷与隐秘     (7项: 体操/骑乘/隐匿/偷窃/开锁/专注/耐力)
├── 社交与表演     (4项: 欺瞒/恐吓/说服 + 歌唱/舞蹈 + 自定义)
├── 知识与学术     (14项: 宗教/调查/估价/逻辑/学习/魔法/炼金... + 自定义)
└── 感知与觉察     (4项: 洞悉/聆听/察觉/警惕值)
```

每项技能的结构：`[id, label, stat]`
- `id`: 唯一标识符（如 `saveStr`, `knoInvestigate`）
- `label`: 显示名称（如 "STR豁免"）
- `stat`: 关联属性（如 `str`, `int`），用于显示修正缩写

#### 渲染方式

`buildSkillLists()`:
1. 将 6 个类别分两栏（左 3 类 + 右 3 类）
2. 每个类别渲染为 `.skill-cat`，标题带颜色标签
3. 每项技能渲染为一行：
   ```
   [checkbox] [技能名称] [数值输入框] [属性缩写]
   ```
4. 自定义项用 `input.sk-custom` 取代名称标签
5. "其他熟练项"区域用 `contentEditable` 标签

#### DM 模式下的技能编辑

当 `dmMode = true` 时（`toggleCharacterDM(true)`）：
- 技能名称从只读 `<span>` 切换为可编辑 `<input.sk-name-dm>`
- DM 可以修改技能名称（紫色虚线边框标识）

#### 搜索与过滤

`filterSkills()`:
- 输入关键词 → 按名称/自定义值过滤
- "隐藏空类别" → 隐藏无匹配项的整个类别
- 重置时恢复所有行显示

---

### 3.7 区块 6 — 财产 · 负重 · 装备

#### 主神空间财产

| 字段 | 输入类型 | DM 批量操作 |
|------|---------|------------|
| 经验值 (mainXp) | number | ±500 按钮 |
| 成就点 (mainAp) | number | ±10 按钮 |

#### 铭牌系统

6 阶铭牌：一阶～六阶，各阶显示独立数量输入框。
- **合成升级**: 3 个同阶 + 20XP → 1 个高阶
- **拆分解锁**: 1 个高阶 → 3 个低阶

#### 负重计算 (`updateLoadCalc()`)

```
承重熟练值 = 承重技能勾选 ? 承重技能点数 : 0
基础负重值 = STR + 承重熟练值

常规负重 = 基础负重值 × 5   (≤ 此值: 轻载)
满载负重 = 基础负重值 × 10  (≤ 此值: 中载, 敏捷-2)
极限负重 = 基础负重值 × 15  (≤ 此值: 重载, 敏捷-5, 速度-50%)
                           (> 此值: 超载, 无法移动)

当前负重 = Σ(物品重量 × 数量)
```

进度条 + 状态文字联动显示。

#### 装备

- 主手武器 / 副手武器 / 防具 — 文本输入
- 物品清单 — 可增删改的表格（名称/数量/重量）

#### 技能槽位

- **主动技能**: 最多 4 个，每个有名称/施展时间/距离/持续时间/疲劳消耗/类别
- **被动技能**: 最多 2 个，每个有名称/类别/阶段

---

### 3.8 加载与保存的完整生命周期

```
加载: loadCharacterIntoUI(name, data)
  │
  ├── 基础身份: 遍历 #identityGrid 的 [data-save] → data.identity[k]
  ├── 核心属性: ABILITY_IDS.forEach → data.stats[id] → onStatChange
  ├── 衍生属性: #derivedGrid input[data-save] → data.derived[k]
  ├── HP/疲劳: #st_hpCur, #st_hpMax → onStatusChange
  ├── 技能: 遍历 _skillWidgets → data.skills[id]
  ├── 其他熟练项: #otherProfsDiv → data.otherProfs
  ├── 背景: querySelector [data-save="bgXxx"] → data.background[k]
  ├── 道果: _talents / _enhances → renderTags
  ├── 财产: _mainXp / _mainAp → 更新输入框
  ├── 经验值条: updateXpBar
  ├── 铭牌: _badges → buildBadgeGrid
  ├── 装备: weaponMain / weaponOff / armor
  ├── 货币: _currencies → buildCurrencyGrid
  ├── 物品清单: _inventory → renderInventory
  ├── 技能槽: _activeSlots / _passiveSlots → renderActiveSlots / renderPassiveSlots
  ├── 生存状态参考: data.statusDetail? → 设置下拉框 → updateStatusEffect
  └── 负重: updateLoadCalc

保存: gatherCharData()
  └── 反向流程: 收集所有 UI 元素 → 组装为 data 对象
```

---

## 四、DM 模式

### 4.1 切换机制

`toggleDM()`:
1. 翻转 `dmMode` 布尔值
2. 切换 `body.dm-mode` CSS class
3. 显示/隐藏 DM 专用按钮 (`#dmPageBtn`)
4. 调用 `toggleCharacterDM()` — 切换技能名称编辑模式
5. 自动导航到 DM 页面

### 4.2 DM 仪表盘

`renderDmDashboard()` — 嵌入角色页面的可折叠面板：

```
┌─ DM 全团概览 ──────────────────────────┐
│  [全团+100 EXP] [全团+500 EXP] [全团+10 AP] │
│  ☐ 仅显示异常                             │
├─────────────────────────────────────────┤
│  ┌── 角色1 ──┐  ┌── 角色2 ──┐           │
│  │ Lv.3     │  │ Lv.5     │           │
│  │ HP 28/36 │  │ HP 45/50 │           │
│  │ ████░░   │  │ ██████░  │           │
│  │ 转职中 ⚠️ │  │ 已转职   │           │
│  └──────────┘  └──────────┘           │
└─────────────────────────────────────────┘
```

- 每张卡片显示角色等级、血脉、职业、HP（含进度条）
- 异常状态自动高亮（HP<30% 红色边框，转职中橙色）
- "仅显示异常"筛选模式

### 4.3 DM 全览页面

`renderDmOverview()` — DM 独立页面 `#dmPage`：
- 每个角色的完整信息卡片（属性、技能、装备、财产）
- 批量操作按钮

---

## 五、商店与剑侠世界

### 5.1 商店页面 (`#shopPage`)

**数据加载**:
1. 优先 `electronAPI.loadShop()` (Electron)
2. 降级 `loadJSON("./data/shop_items.json")` (fetch)
3. 最终 fallback 5 件硬编码商品

**渲染**: `renderShopTable()`
- 合并主神空间 + 剑侠物品为统一列表
- 按类型/关键词过滤
- 每行显示：类型(色标)、阶位、名称、描述、经验值、成就点、铭牌
- 当前角色有购买按钮（🛒）

**购买流程** (`buyShopItem`):
1. 检查经验值/成就点是否足够
2. 扣除资源
3. 添加物品到角色背包
4. 自动保存

### 5.2 剑侠世界页面 (`#jianxiaPage`)

独立世界观浏览页面，与商店共享数据源但独立渲染：
- 按类型（功法/天材地宝/神兵利器）和品质筛选
- 显示门派、类别、学习费用
- 数据来自 `jianxia_items.json`，含 19 件商品

---

## 六、文件依赖关系

```
index.html
  ├── styles/theme.css
  ├── scripts/app.js          (导航 + 数据 I/O + DM 开关)
  ├── scripts/character.js    (角色面板 + DM 面板)
  └── scripts/shop.js         (商店 + 剑侠)

data/
  ├── characters.json         ← app.js / character.js 读写
  ├── shop_items.json         ← shop.js 读取
  ├── jianxia_items.json      ← shop.js 读取
  └── status_refs/            ← character.js 读取
       ├── bloodline.json
       ├── race.json
       ├── base_class.json
       └── promotion.json

跨文件共享:
  - loadJSON()            定义于 character.js，shop.js 使用
  - characters {}         定义于 app.js，character.js + shop.js 读写
  - navigateTo()          定义于 app.js，所有页面 onclick 调用
  - showToast()           定义于 character.js, app.js importData 调用
  - DM 相关函数           定义于 character.js, app.js toggleDM 调用
```

## 七、关键设计决策

1. **全局变量架构**: 所有数据存储在全局对象（`characters`、`_shopItems`、`_bloodlineData` 等），非模块化。适合小团队快速迭代。
2. **单一数据源**: `gatherCharData()` 是唯一的保存函数，确保数据结构一致性。
3. **自动保存**: 任何字段变更后 500ms 防抖自动保存，无需手动点击。
4. **渐进增强**: `loadJSON()` 优先使用 Electron API，降级到 fetch，再降级到内联默认数据。
5. **DM/玩家双模式**: 同一页面通过 CSS class 切换，DM 模式解锁技能编辑和批量操作。
6. **视觉反馈**: 保存 Toast、HP 预警闪烁、负重色标变化、加载状态文字。
