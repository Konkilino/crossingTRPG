# 穿越团 TRPG 终端 · 修改计划

## 总体目标

- 弃用 Electron，采用纯 HTTP 方案
- 建立统一数据层 `dataService.js`，所有数据读写走这里
- 修复现有 bug（角色数据不加载、商店/剑侠页面打不开、血脉状态不关联）
- 保持可扩展性——新增世界只需加 JSON 文件 + 一行配置

---

## 架构变更

改前: main.js (Electron) + preload.js → window.electronAPI → IPC → 读/写 data/*.json
      server.js (HTTP 服务器，只读)

改后: dataService.js (统一数据层)
          ├── 读: fetch(`./data/*.json`)
          └── 写: fetch(`/api/data/*`, { method: 'POST' })
                       ↕
                 server.js (HTTP 服务器，读写兼备)

---

## 修改步骤（共 10 步）

### 第 1 步：server.js — 增加数据写入 API
- 新增 POST /api/data/:filename 路由
- 请求体：JSON 数据，文件路径
- 安全限制：禁止目录穿越
- 写入 data/ 目录

### 第 2 步：新建 scripts/dataService.js — 统一数据层
函数列表：loadJSON(path), saveJSON(path, data)

### 第 3 步：index.html — 移除 Electron 引用 + 修复 CSS
- 在末尾引入 dataService.js
- 补全剑侠按钮 hover 样式
- 补全剑侠页面行悬停样式

### 第 4 步：app.js — 重写数据层
- 所有 window.electronAPI 引用删除
- loadAllData/saveAllData 改用 dataService

### 第 5 步：character.js — 修复 populateSelect + 数据加载
- 删除内部 loadJSON，改用 dataService
- 修复 populateSelect 过早 dispatch change 事件
- 增加 _statusRefsLoaded 标记

### 第 6 步：shop.js — 去重 + 重构
- loadJianxiaData 不再重复 fetch
- 统一走 dataService

### 第 7 步：调整 CSS 样式 (theme.css)
- 剑侠页面专属样式
- jianxia 按钮 hover

### 第 8 步：删除无用文件
- main.js, preload.js
- node_modules 目录

### 第 9 步：更新 package.json
- 移除 electron 依赖
- 添加 start 脚本

### 第 10 步：端到端验证

---

## 可扩展性设计

以后新增世界（如 "星际迷航"），只需：
1. 新建 data/star_trek_items.json
2. 在 index.html 主页加一个按钮
3. 在 navigateTo() 中加 if (page === 'startrek') ...
4. 数据层的 loadJSON 自动生效
